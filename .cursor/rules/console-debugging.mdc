---
description: Console Demo 日志分析和帧同步调试规则
globs: src/AbilityKit.Demo.Moba.Console/**/*.cs
alwaysApply: false
---

# Console Demo 调试规则

## 日志频道

| 频道 | 说明 | 优先级 |
|-----|------|-------|
| `[SYS]` | 系统日志 | System |
| `[PHASE]` | 阶段切换 | System |
| `[BATTLE]` | 战斗日志 | Battle |
| `[SKILL]` | 技能日志 | Skill |
| `[DAMAGE]` | 伤害日志 | Damage |
| `[SYNC]` | 同步日志 | Sync |
| `[INPUT]` | 输入日志 | Input |
| `[TRACE]` | 追踪日志 | Trace |

## 帧同步状态检查

运行时关注 `[SYNC]` 日志：

```
[SYNC] [Sync] Frame: 539, State: InMatch, Actors: 7
```

正常状态：
- Frame 持续增长
- State = InMatch
- Actors = 预期数量

## 常见错误模式

### 技能配置未加载

```
[WARN] [SkillExecutor] Skill config not found: 10010101
```

**原因**：`characters.json` 中的 `SkillIds` 与 `skills.json` 中的 `Id` 不匹配

**修复**：确保 `skills.json` 包含角色所需的所有技能 ID

### 阶段状态异常

```
[TRACE] ConsoleInputFeature.Tick - State: Idle
```

**原因**：`Context.State` 未更新为 `InMatch`

**排查**：检查 `PhaseHost.PhaseEntered` 事件是否正确传播

### 冷却不生效

连续点击技能后仍能释放：

**排查**：
1. 检查 `skills.json` 中 `CooldownMs` 是否 > 0
2. 检查 `MobaConfigDatabase.TryGetSkill()` 是否返回正确配置
3. 检查 `_cooldownBySlot` 是否正确设置

## 调试工作流

1. 运行 `dotnet run` 并等待自动测试完成
2. 检查测试结果：`7/7 tests passed`
3. 如有问题，用 `Log.EnableTrace()` 启用追踪模式
4. 分析 `[TRACE]` 日志定位问题

## 自动测试验证点

| 测试 | 验证内容 |
|-----|---------|
| Init | Bootstrapper、Flow、Services、ECS 初始化 |
| Phase | InMatch 阶段正确进入 |
| FrameSync | Frame 持续增长 |
| SkillCast | 技能成功释放 |
| Damage | 伤害正确计算 |
| Cooldown | 冷却时间生效 |
| Move | 移动指令正确执行 |
