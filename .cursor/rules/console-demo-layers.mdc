# Console Demo 层级架构规范

## 概述

本文档定义 Console Demo 项目中 **逻辑层** 和 **表现层** 的职责边界。

---

## 一、核心原则

```
┌─────────────────────────────────────────────────────────────────────┐
│                         层级架构原则                                  │
│                                                                     │
│   1️⃣ 逻辑层负责计算，表现层负责渲染                                  │
│      - 逻辑层生成最终状态                                            │
│      - 表现层只渲染结果                                              │
│                                                                     │
│   2️⃣ 事件携带完整状态                                               │
│      - 事件应包含渲染所需的所有数据                                  │
│      - 表现层不做任何计算                                            │
│                                                                     │
│   3️⃣ 单向数据流                                                     │
│      输入 → 表现层收集 → 逻辑层计算 → 事件发布 → 表现层渲染         │
│                                                                     │
│   4️⃣ 表现层是纯渲染层                                               │
│      - ❌ 不持有逻辑层服务引用                                       │
│      - ❌ 不计算任何游戏数值                                         │
│      - ✅ 只订阅事件并调用渲染 API                                    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 二、正确的架构边界

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           正确的架构边界                                   │
│                                                                             │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                      表现层 (Console Demo)                             │   │
│   │                                                                    │   │
│   │   ConsoleBattleBootstrapper     - 组装组件，管理生命周期              │   │
│   │   ConsoleBattleView            - ASCII 渲染                          │   │
│   │   ConsoleBattleViewEventSink   - 实现 IBattleViewEventSink           │   │
│   │   ConsoleInputFeature         - 输入采集                             │   │
│   │   DirectCallInputSink         - 实现 IWorldInputSink，转发输入       │   │
│   │                                                                    │   │
│   │   职责: 只负责渲染和输入采集，不处理任何游戏逻辑                      │   │
│   │                                                                    │   │
│   │   ❌ 禁止:                                                         │   │
│   │     - 计算伤害、Buff、HP 等游戏数值                                  │   │
│   │     - 持有 SkillExecutor、MobaEffectExecutionService 等引用           │   │
│   │     - 直接访问 ECS 世界获取游戏状态                                 │   │
│   │                                                                    │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                      │                                    │
│                                      │ 事件 (携带完整状态)                │
│                                      ▼                                    │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                      逻辑层 (AbilityKit.Demo.Moba)                   │   │
│   │                                                                    │   │
│   │   MobaLobbyInputSink          - 处理 Move/SkillInput 命令            │   │
│   │   SkillExecutor               - 技能执行                             │   │
│   │   MobaEffectExecutionService  - 效果执行、伤害计算                   │   │
│   │   MobaActorTransformSnapshotService  - 位置快照                      │   │
│   │   MobaDamageEventSnapshotService   - 伤害快照                      │   │
│   │   FrameSnapshotDispatcher     - 分发快照事件                        │   │
│   │                                                                    │   │
│   │   职责: 所有计算、判定、状态管理                                    │   │
│   │                                                                    │   │
│   │   ✅ 正确:                                                         │   │
│   │     - 计算所有游戏数值（伤害、Buff、HP 等）                         │   │
│   │     - 发布完整的事件（携带所有渲染所需数据）                         │   │
│   │     - 管理 ECS 世界状态                                            │   │
│   │                                                                    │   │
│   └───────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、组件职责边界

### 3.1 DirectCallInputSink - 只做转发

```csharp
/// <summary>
/// 表现层输入转发器
/// 实现 IWorldInputSink 接口，将输入转发到逻辑层
/// </summary>
public sealed class DirectCallInputSink : IWorldInputSink
{
    /// <summary>
    /// 提交输入命令
    /// 只是转发，不做任何处理
    /// </summary>
    public void Submit(FrameIndex frame, IReadOnlyList<PlayerInputCommand> inputs)
    {
        if (inputs == null || inputs.Count == 0) return;

        // ✅ 正确：只是转发输入
        Platform.Log.Input($"[DirectCallInputSink] Submit {inputs.Count} commands at frame {frame}");
    }

    /// <summary>
    /// ❌ 错误示例：在 DirectCallInputSink 中处理逻辑
    /// </summary>
    // public void Submit(FrameIndex frame, IReadOnlyList<PlayerInputCommand> inputs)
    // {
    //     foreach (var cmd in inputs)
    //     {
    //         if (cmd.OpCode == MobaOpCode.SkillInput)
    //         {
    //             _skillExecutor.CastBySlot(...);  // ❌ 错误：处理逻辑
    //         }
    //     }
    // }
}
```

### 3.2 ConsoleBattleViewEventSink - 只做渲染

```csharp
/// <summary>
/// 表现层视图事件接收器
/// 实现 IBattleViewEventSink 接口，只订阅事件并渲染
/// </summary>
public sealed class ConsoleBattleViewEventSink : BaseBattleViewEventSink
{
    private readonly IConsoleBattleView _battleView;

    protected override void OnDamageEvent(...)
    {
        // ✅ 正确：只做渲染，不做计算
        _battleView.ShowFloatingText(targetId, $"-{damageValue}", false);
        _battleView.UpdateEntityHp(targetId, targetHpAfter, defaultMaxHp);

        if (isKill)
        {
            _battleView.ShowFloatingText(targetId, "DIED!", false);
        }
    }

    /// <summary>
    /// ❌ 错误示例：在 ViewEventSink 中计算
    /// </summary>
    // protected override void OnDamageEvent(...)
    // {
    //     var finalDamage = damageValue * armorReduction;  // ❌ 错误：计算护甲
    //     var newHp = targetHp - finalDamage;              // ❌ 错误：计算 HP
    //     _battleView.UpdateEntityHp(targetId, newHp, maxHp);
    // }
}
```

### 3.3 逻辑层 - 完整计算

```csharp
/// <summary>
/// 逻辑层服务示例
/// 负责所有计算，发布完整的事件
/// </summary>
public sealed class MobaEffectExecutionService
{
    public void Execute(int effectId, IAbilityPipelineContext context)
    {
        // ✅ 正确：在逻辑层计算所有数值
        var damage = CalculateDamage(caster, target, skillLevel);
        var finalDamage = ApplyArmorReduction(damage, targetArmor);
        var newHp = Math.Max(0, target.Hp - finalDamage);
        var isDead = newHp <= 0;

        // ✅ 正确：发布完整的事件
        var damageData = new DamageEventData(
            casterId: caster.Id,
            targetId: target.Id,
            damageType: skillDamageType,
            damageValue: finalDamage,        // 完整计算后的伤害
            targetHpAfter: newHp,           // 计算后的 HP
            maxHp: target.MaxHp,
            isDead: isDead);                // 死亡判定结果

        _snapshotDispatcher.DispatchDamageEvent(currentFrame, new[] { damageData });
    }
}
```

---

## 四、完整数据流

```
输入 → ConsoleInputFeature → 逻辑层 → FrameSnapshotDispatcher → ConsoleBattleViewEventSink → ConsoleBattleView
```

---

## 五、Feature + ModuleHost 架构

### 5.1 架构概述

Console Demo 使用 **FeatureHost + ModuleHost** 模式管理组件生命周期，支持嵌套层级结构。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           FeatureHost                                        │
│   管理 Feature 生命周期和依赖排序                                             │
│                                                                             │
│   ┌───────────────────────────────────────────────────────────────────┐   │
│   │                  ConsoleViewFeature (Feature)                          │   │
│   │   实现 IConsoleViewFeatureModulesHost                                │   │
│   │                                                                      │   │
│   │   ┌─────────────────────────────────────────────────────────────┐ │   │
│   │   │           ModuleHost<IConsoleViewFeatureModulesHost,           │ │   │
│   │   │                    IConsoleViewModule>                        │ │   │
│   │   │                                                             │ │   │
│   │   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │ │   │
│   │   │   │ Binding     │  │ EventSink   │  │ Timeline    │       │ │   │
│   │   │   │ Module      │  │ Module      │  │ Module      │       │ │   │
│   │   │   └─────────────┘  └─────────────┘  └─────────────┘       │ │   │
│   │   │                                                             │ │   │
│   │   │   ┌─────────────────────────────────────────────────┐     │ │   │
│   │   │   │       CompositeModule (支持嵌套)                   │     │ │   │
│   │   │   │       ┌───────────────────────────────────────┐   │     │ │   │
│   │   │   │       │  子 ModuleHost                        │   │     │ │   │
│   │   │   │       │  ┌─────────┐ ┌─────────┐             │   │     │ │   │
│   │   │   │       │  │Child A  │ │Child B  │             │   │     │ │   │
│   │   │   │       │  └─────────┘ └─────────┘             │   │     │ │   │
│   │   │   │       └───────────────────────────────────────┘   │     │ │   │
│   │   │   └─────────────────────────────────────────────────┘     │   │
│   │   └─────────────────────────────────────────────────────────────┘   │
│   └───────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 核心接口

```csharp
// Feature Host 接口
public interface IFeature : IModuleId, IModuleDependencies
{
    void OnAttach(IFeatureContext ctx);
    void OnDetach(IFeatureContext ctx);
}

// Module Host 管理器
public sealed class ModuleHost<TContext, TModule> : IDisposable
    where TContext : class
    where TModule : class
{
    ModuleHost<TContext, TModule> Add(TModule module);
    void Attach(in TContext context);
    void Detach(in TContext context);
    void Tick(in TContext context, float deltaTime);
    void RebindAll(in TContext context);
}

// Module 接口 - 继承所有必要的接口
public interface IConsoleViewModule : 
    IModuleId,                          // 模块标识
    IModuleDependencies,                 // 模块依赖
    IGameModule<IConsoleViewFeatureModulesHost>,      // 生命周期
    IGameModuleTick<IConsoleViewFeatureModulesHost>,  // Tick 支持
    IGameModuleRebind<IConsoleViewFeatureModulesHost>  // Rebind 支持
{
}

// Host 接口 - 暴露能力给 Module
public interface IConsoleViewFeatureModulesHost
{
    ConsoleBattleContext Context { get; }
    ConsoleSessionHooks? Hooks { get; }
    IConsoleViewBinder Binder { get; }
    IConsoleBattleView BattleView { get; }
    ConsoleVfxManager VfxManager { get; }
    ConsoleBattleViewEventSink EventSink { get; }
}
```

### 5.3 Module 示例

```csharp
/// <summary>
/// Binding Module - 管理 ViewBinder 生命周期
/// </summary>
public sealed class ConsoleBindingModule : IConsoleViewModule
{
    private const string ModuleId = "view_binding";
    private IConsoleViewBinder? _binder;

    public string Id => ModuleId;
    public string[]? Dependencies => null;  // 无依赖

    public void OnAttach(IConsoleViewFeatureModulesHost host)
    {
        _binder = new ConsoleViewBinder();
        host.RegisterBinder(_binder);
        // 触发事件...
    }

    public void OnDetach(IConsoleViewFeatureModulesHost host)
    {
        host.UnregisterBinder(_binder!);
        _binder.Dispose();
    }

    public void Tick(IConsoleViewFeatureModulesHost host, float deltaTime) { }
    public void Rebind(IConsoleViewFeatureModulesHost host) { }
}

/// <summary>
/// 支持嵌套的 Composite Module
/// </summary>
public sealed class ConsoleCompositeModule : IConsoleViewModule
{
    private const string ModuleId = "composite_parent";
    
    // 子 ModuleHost
    private readonly ModuleHost<IConsoleViewFeatureModulesHost, IConsoleViewModule> _childHost = new();
    private IConsoleViewFeatureModulesHost? _host;

    public string Id => ModuleId;
    public string[]? Dependencies => null;

    public void OnAttach(IConsoleViewFeatureModulesHost host)
    {
        _host = host;
        
        // 添加子模块
        _childHost.Add(new ConsoleChildModuleA());
        _childHost.Add(new ConsoleChildModuleB());
        
        // 附加子模块
        _childHost.Attach(host);
    }

    public void OnDetach(IConsoleViewFeatureModulesHost host)
    {
        _childHost.Detach(host);
        _host = null;
    }

    public void Tick(IConsoleViewFeatureModulesHost host, float deltaTime)
    {
        _childHost.Tick(_host!, deltaTime);
    }

    public void Rebind(IConsoleViewFeatureModulesHost host)
    {
        _childHost.RebindAll(_host!);
    }
}
```

### 5.4 ViewFeature 使用 ModuleHost

```csharp
public sealed class ConsoleViewFeature : 
    IGameModule<ConsoleBattleContext>, 
    IGamePhaseFeature, 
    IConsoleViewFeatureModulesHost
{
    // 使用 ModuleHost 管理所有 Module
    private readonly ModuleHost<IConsoleViewFeatureModulesHost, IConsoleViewModule> _moduleHost = new();

    public void OnAttach(ConsoleBattleContext context)
    {
        // 注册 Module
        _moduleHost.Add(new ConsoleBindingModule());
        _moduleHost.Add(new ConsoleEventSinkModule());
        _moduleHost.Add(new ConsoleTimelineModule());
        // ...

        // 附加所有 Module（自动处理依赖排序）
        _moduleHost.Attach(this);
    }

    public void Tick(in ConsoleGamePhaseContext ctx, float deltaTime)
    {
        // Tick 所有 Module（包括嵌套的子模块）
        _moduleHost.Tick(this, deltaTime);
    }

    public void OnDetach(ConsoleBattleContext context)
    {
        // 分离所有 Module（反向顺序）
        _moduleHost.Detach(this);
    }
}
```

### 5.5 设计原则

| 原则 | 说明 |
|-----|------|
| **单一职责** | 每个 Module 只负责一个功能 |
| **依赖注入** | Module 通过 Host 接口获取能力 |
| **生命周期管理** | ModuleHost 自动处理 Attach/Detach 顺序 |
| **支持嵌套** | Module 可以包含自己的子 ModuleHost |
| **无循环依赖** | 依赖关系必须是 DAG |

### 5.6 禁止事项

| 禁止 | 说明 |
|-----|------|
| ❌ Module 直接引用其他 Module | 通过 Host 接口通信 |
| ❌ Module 持有自己的 ModuleHost 的引用 | 由父级管理 |
| ❌ 在构造函数中初始化 | 使用 OnAttach |
| ❌ 循环依赖 | 依赖关系必须是有向无环图 |
