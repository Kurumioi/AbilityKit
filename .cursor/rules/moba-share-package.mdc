# Moba Share 包职责与设计规范

## 包定位

`com.abilitykit.demo.moba.share` 是 MOBA 战斗系统的**表现层抽象包**，定义平台无关的接口和数据结构，供不同平台（Unity/Console/Server）实现。

## 核心职责

### 1. 定义接口契约

Share 包定义**纯抽象接口**，不包含任何实现：

```
✅ 正确：定义 IInterface 接口
❌ 错误：在 share 包中实现任何逻辑
```

### 2. 定义数据结构

Share 包定义**平台无关的数据结构**（struct），用于跨层数据传输：

```csharp
✅ 正确：FrameSnapshotData, ActorTransformData, TriggerEventData
❌ 错误：包含任何 Unity 特有类型（GameObject, Transform, Vector3）
```

### 3. 定义枚举和常量

Share 包定义**业务枚举和事件码**：

```csharp
✅ 正确：SyncMode, HostMode, SessionState, BattleEventCode
❌ 错误：Unity 特有的 Editor 枚举或渲染枚举
```

## 架构分层

```
┌─────────────────────────────────────────────────────────────┐
│                    Platform Layer (平台层)                    │
│  ┌─────────────────┐     ┌─────────────────┐               │
│  │ Unity.View.Runtime │     │ Console.Runtime    │               │
│  │   (实现接口)       │     │   (实现接口)       │               │
│  └─────────────────┘     └─────────────────┘               │
└─────────────────────────────┬───────────────────────────────┘
                              │ 实现接口
┌─────────────────────────────▼───────────────────────────────┐
│                      Share Layer (表现层)                      │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Interfaces: IViewEventSink, IFrameDeserializer, etc.    ││
│  │ Structs: FrameSnapshotData, ActorTransformData, etc.     ││
│  │ Enums: SyncMode, HostMode, SessionState, etc.           ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────┬───────────────────────────────┘
                              │ 使用数据结构
┌─────────────────────────────▼───────────────────────────────┐
│                    Logic Layer (逻辑层)                       │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ BattleLogicSession                                       ││
│  │ - 每帧计算 (Frame Tick)                                  ││
│  │ - 技能系统、伤害结算、移动、碰撞                           ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

## 目录结构规范

```
com.abilitykit.demo.moba.share/Runtime/Game/Flow/Battle/
├── Snapshot/              # 帧同步相关
│   ├── FrameSnapshotData.cs           # 快照数据结构
│   ├── IFrameSnapshotDeserializer.cs   # 反序列化器接口
│   ├── IFrameSnapshotAssembler.cs      # 组装器接口
│   └── IFrameSnapshotDispatcher.cs     # 分发器接口
├── Session/               # 会话管理相关
│   ├── ISessionController.cs           # 会话控制器
│   ├── ISessionOrchestrator.cs         # 编排器接口
│   └── BattleStartPlan.cs              # 启动计划
├── ViewEvents/            # 视图事件相关
│   ├── IBattleViewEventSink.cs         # 视图事件接收器
│   ├── IViewSnapshotReceiver.cs        # 快照接收器
│   └── IFrameSeekableView.cs           # 帧可定位视图
├── View/                  # 视图相关
│   └── IBattleView.cs                 # 战斗视图接口
├── Network/               # 网络相关
│   ├── INetworkEventSender.cs         # 网络事件发送器
│   ├── INetworkFrameReceiver.cs       # 网络帧接收器
│   └── BattleEventCode.cs            # 事件码
└── Interfaces/            # 平台抽象接口
    ├── IViewFactory.cs               # 视图工厂
    ├── IHudPresentation.cs           # HUD 表现
    └── IPlatformInputSource.cs       # 平台输入源
```

## 命名规范

### 接口命名

| 前缀 | 用途 | 示例 |
|------|------|------|
| `I` | 接口 | `IBattleViewEventSink` |
| `I` + `T` | 泛型接口 | `IFrameSnapshotDeserializer<T>` |
| `On` | 事件处理方法 | `OnEnterGameSnapshot` |
| `Dispatch` | 分发方法 | `DispatchActorTransform` |
| `Try` + 动词 | 尝试方法 | `TryDeserializeEnterGame` |

### 结构体命名

- 使用 `Data` 后缀表示数据传输对象
- 使用 `Event` 后缀表示事件数据
- 使用 `Config` 后缀表示配置数据

```csharp
✅ 正确：FrameSnapshotData, TriggerEventData, DamageEventData
❌ 错误：ActorTransform（缺少 Data 后缀）
```

### 枚举命名

| 类型 | 命名方式 | 示例 |
|------|----------|------|
| 枚举类型 | PascalCase | `SyncMode`, `SessionState` |
| 枚举值 | PascalCase | `SyncMode.Lockstep`, `SessionState.Running` |
| 事件码常量 | PascalCase + 类别前缀 | `FrameSnapshot`, `SkillCast` |

## 禁止事项

### 绝对禁止

❌ **禁止**：在 Share 包中包含任何 Unity 特有类型
- 禁止：`UnityEngine.GameObject`, `UnityEngine.Transform`, `UnityEngine.Vector3`
- 禁止：`UnityEngine.UI.*`, `UnityEngine.EventSystems.*`
- 解决方案：使用 `AbilityKit.Core.Math.Vec3` 或自定义 struct

❌ **禁止**：在 Share 包中包含任何 Console 特有类型
- 禁止：`System.Console` 相关逻辑
- 禁止：特定于终端的渲染逻辑

❌ **禁止**：在 Share 包中实现任何具体逻辑
- 禁止：任何继承自 `MonoBehaviour` 的类
- 禁止：任何包含协程的类
- 禁止：任何依赖特定平台的代码

❌ **禁止**：在 Share 包中引用 view 包或 logic 包
- Share 包应该是最底层的依赖
- 其他包可以依赖 Share 包

### 依赖规则

```
Share 包依赖：
├── AbilityKit.Core (可选，框架级类型)
├── AbilityKit.Ability.Triggering (可选，触发器事件)
└── System (标准库)

不应依赖：
├── com.abilitykit.demo.moba.view.runtime
├── com.abilitykit.demo.moba.logic
└── 任何 Unity/Console 特有程序集
```

## 实现指南

### 1. 定义接口时使用平台无关类型

```csharp
// ✅ 正确：使用平台无关的数据结构
public interface IBattleViewEventSink
{
    void OnActorTransformSnapshot(in FrameSnapshotData snapshot);
}

// ❌ 错误：使用 Unity 特有类型
public interface IBattleViewEventSink
{
    void OnActorSpawn(GameObject actor); // 禁止！
}
```

### 2. 优先使用 readonly struct

```csharp
// ✅ 正确：使用 readonly struct 提高性能
public readonly struct ActorTransformData
{
    public int ActorId { get; }
    public float PositionX { get; }
    // ...
}

// ❌ 错误：使用 class
public class ActorTransformData { } // 禁止！
```

### 3. 使用 in 参数避免拷贝

```csharp
// ✅ 正确：使用 in 参数
void OnActorTransformSnapshot(in FrameSnapshotData snapshot);

// ❌ 错误：值拷贝
void OnActorTransformSnapshot(FrameSnapshotData snapshot); // 不必要的拷贝
```

### 4. 事件方法命名统一使用 On 前缀

```csharp
public interface IBattleViewEventSink
{
    void OnEnterGameSnapshot(in FrameSnapshotData snapshot);    // ✅
    void OnActorTransformSnapshot(in FrameSnapshotData snapshot); // ✅
    void OnDamageEventSnapshot(in FrameSnapshotData snapshot);   // ✅
}
```

## Share 包与 View 包的关系

### Share 包职责（纯抽象）

- 定义 `IBattleViewEventSink` 接口
- 定义 `FrameSnapshotData` 等数据结构
- 定义 `IFrameSnapshotDeserializer` 反序列化接口

### View 包职责（具体实现）

- 实现 `IBattleViewEventSink` 接口，创建 GameObject/VFX
- 实现 `IFrameSnapshotDeserializer`，解析 protobuf/二进制数据
- 管理 GameObject 生命周期

### 迁移策略

| 原 view.runtime 代码 | 迁移到 share | 保留在 view.runtime |
|---------------------|--------------|---------------------|
| `IBattleViewEventSink` | ✅ 接口 | - |
| `FrameSnapshotData` 相关 | ✅ 数据结构 | - |
| `BattleViewEventSink` | - | 继承 `IBattleViewEventSink` |
| `BattleViewFactory` | ✅ 抽象 | 具体 Prefab 实例化逻辑 |
| `BattleSnapshotApplier` | ✅ 基类 | 具体应用逻辑 |

## 版本管理

Share 包遵循语义化版本：

- **主版本**：不兼容的架构变更
- **次版本**：向后兼容的功能添加
- **补丁版本**：向后兼容的 bug 修复

## 总结

| 维度 | Share 包职责 |
|------|-------------|
| **是** | 定义纯抽象接口 |
| **是** | 定义平台无关数据结构 |
| **是** | 定义业务枚举和常量 |
| **是** | 可被所有平台和服务器引用 |
| **否** | 包含任何实现逻辑 |
| **否** | 包含任何 Unity/Console 特有类型 |
| **否** | 依赖 view 或 logic 包 |
