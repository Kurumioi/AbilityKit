# 日志使用规范

## 核心原则

**禁止使用 `UnityEngine.Debug.Log`**（`Debug.Log`, `Debug.LogWarning`, `Debug.LogError`）。必须使用 `AbilityKit.Core.Common.Log.Log` 工具类。

## Log 类接口

```csharp
using AbilityKit.Core.Common.Log;

// 信息日志 - 一般流程信息
Log.Info("message");

// 警告日志 - 异常但可恢复的情况
Log.Warning("message");

// 错误日志 - 错误或失败
Log.Error("message");

// 异常日志 - 捕获的异常（自动包含堆栈信息）
Log.Exception(ex, "[Context] message");
```

## 使用场景

| 方法 | 使用场景 | 示例 |
|-----|---------|-----|
| `Log.Info` | 重要流程节点、状态转换、成功操作 | 进入战斗、连接成功、技能释放 |
| `Log.Warning` | 可恢复的异常、参数校验失败、资源未就绪 | 服务未注册、配置缺失 |
| `Log.Error` | 操作失败、致命错误 | 创建实体失败、序列化错误 |
| `Log.Exception` | 捕获的异常（自动记录堆栈） | try-catch 块中 |

## 禁用模式

```csharp
// ❌ 禁止 - 直接使用 Unity Debug
UnityEngine.Debug.Log("message");
Debug.LogWarning("warning");
Debug.LogError("error");

// ✅ 正确 - 使用 Log 工具类
Log.Info("message");
Log.Warning("warning");
Log.Error("error");
Log.Exception(ex, "[Context] operation failed");
```

## 命名空间

使用以下命名空间之一：

```csharp
// MOBA Runtime/Share 包
using AbilityKit.Core.Common.Log;

// 或使用全名
AbilityKit.Core.Common.Log.Log.Info("message");
```

## 日志级别说明

### Info 级别
- 流程入口/出口
- 状态转换
- 初始化完成
- 配置参数（仅关键参数）

### Warning 级别
- 依赖缺失但可跳过
- 参数校验失败
- 非致命异常恢复
- 资源未就绪

### Error 级别
- 操作明确失败
- 断言失败
- 关键依赖不可用

### Exception 级别
- 所有 catch 块中的异常
- 自动包含 `exception.ToString()` 信息

## 特殊说明

### Debug 包中的调试日志
如果确实需要临时调试日志（仅用于开发调试），可以保留 `Debug.Log`，但：
1. 完成后必须删除或改为合适的 Log 级别
2. 不要提交包含大量 Debug.Log 的代码

### Timeline/Demo 相关
`TimelineLogicRunner`, `SkillPipelineDemoRunner` 等 Demo 类可以使用 `Debug.Log`，因为它们是独立的测试组件。

## 规则文件路径

- 本规则：`c:\Workspace\gitProject\AbilityKit\.cursor\rules\logging.mdc`
